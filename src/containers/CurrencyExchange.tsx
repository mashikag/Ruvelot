import React from 'react';
import { CurrencyExchangeState } from '../store/types';

class CurrencyExchangeContainer
  extends React.Component<CurrencyExchangeState> {

  render(): React.ReactElement<CurrencyExchangeState> {
    return (<div></div>);
  }
}

export default CurrencyExchangeContainer;
